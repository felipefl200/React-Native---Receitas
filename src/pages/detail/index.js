import { View, Text, StyleSheet } from "react-native"

export function Detail() {
  return (
    <View style={styles.container}>
      <Text>Pagina Detail X</Text>
    </View>
  )
}

const styles = StyleSheet.create({
  container: {
    backgroundColor: "blue",
  },
})

export default Detail
